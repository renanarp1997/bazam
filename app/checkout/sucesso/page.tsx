import OrderSuccess from "@/components/OrderSuccess";

export const metadata = {
  title: "Pedido confirmado — Bazam",
};

export default function CheckoutSuccessPage() {
  return <OrderSuccess />;
}
